# Proto Contracts

This directory is the canonical home for protobuf contracts.

Expected service namespaces:

- `auth/v1`
- `fraud/v1`
- `ledger/v1`
- `ml/v1`
- `common/v1`

Code generation and `buf` wiring are tracked by later roadmap items.
