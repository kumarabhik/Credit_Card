# Architecture Decision Records

This directory is reserved for ADRs referenced by `agents.md` and `roadmap.md`.

- `0001-*.md` will record the baseline architecture choices.
- Future ADRs should use zero-padded numeric prefixes and a short slug.
- Do not repurpose or delete historical ADRs; append new decisions instead.
