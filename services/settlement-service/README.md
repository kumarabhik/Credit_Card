# settlement-service

Java settlement and reconciliation worker skeleton.

Expected layout:

- `src/main/java/com/cc/settlement/` for batch close, reconciliation, and chargeback flows
- `src/main/resources/` for Spring Boot configuration
- `src/test/java/com/cc/settlement/` for unit and integration tests
